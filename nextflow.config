process {
  executor = 'sge'
  queue = '40core-192G.q'
  penv = 'smp'
  beforeScript = 'module add apptainer'
  clusterOptions = '-V -l h_rt=02:00:00 -l h_vmem=8G'
}

process {
  executor = 'local'
}
